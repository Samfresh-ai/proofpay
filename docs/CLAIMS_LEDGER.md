# Claims ledger

Last updated: 2026-08-12

Statuses: `PROVED`, `PARTIAL`, `INFERRED`, `PLANNED`, `FALSE`.

| ID | Claim | Status | Evidence or constraint |
| --- | --- | --- | --- |
| RULE-001 | Flare Summer Signal closes on 2026-08-14. | PROVED | Canonical DoraHacks detail page; exact rendered cutoff recorded in `STATUS.md`. |
| RULE-002 | Payment flows using XRP/FXRP or FAssets fit Bounty 1. | PROVED | Canonical DoraHacks track page. |
| RULE-003 | Existing projects and ports are allowed when new work is separated clearly. | PROVED | Canonical event detail page. |
| RULE-004 | Judging uses product usefulness, Flare integration quality, technical execution, evidence of new work, and clarity/future potential. | PROVED | Canonical event detail page; no weights are published. |
| TECH-001 | Coston2 reports chain ID 114 through the selected RPC. | PROVED | Live RPC read recorded in `artifacts/flare-probe.json`. |
| TECH-002 | FXRP can be resolved through the current official discovery mechanism. | PROVED | The Contract Registry resolved `AssetManagerFXRP`; `fAsset()` resolved the token; metadata is recorded in `artifacts/flare-probe.json`. |
| TECH-003 | FTSOv2 returns XRP/USD value, decimals, and feed timestamp on Coston2. | PROVED | The Contract Registry resolved `FtsoV2`; the Phase 1 read is recorded in `artifacts/flare-probe.json`, and 24 further read-only samples are recorded in `artifacts/ftso-tolerance.json`. |
| TECH-004 | A real user-approved FXRP transfer confirms between two test wallets on Coston2. | PROVED | Transaction `0x4c4394cfa2bbd2bed4a5125c4eacb34db8e7bd1a905e2dc9b9cbdf98e6d4503a` succeeded on chain 114 and moved exactly `0.001 FTestXRP`; receipt, log isolation, and before/after balances are recorded in `artifacts/flare-probe.json`. |
| TECH-005 | The pinned Foundry project compiles the official Coston2 registry, AssetManager/FXRP, production FTSOv2, OpenZeppelin SafeERC20, and ReentrancyGuard interfaces needed by the locked design. | PROVED | Historical Phase 2 evidence is preserved in commit `c3e850a`; the production escrow now directly imports FTSOv2, SafeERC20, ReentrancyGuard, and Math under the same pins, and `forge build --force` passes. |
| ARCH-001 | ProofPay has an implementation-ready state machine, authority model, data record, ABI, events, errors, and security invariants for its single-invoice FXRP MVP. | PROVED | `docs/ARCHITECTURE.md`, `docs/CONTRACT_SPEC.md`, `contracts/src/ProofPayEscrow.sol`, 56 deterministic tests, six 512-run fuzz properties, six 128-by-32 stateful invariants, the Phase 4A deployment, and the Phase 4B live receipt cover the specified core. Audit evidence remains absent. |
| ARCH-002 | The Phase 2 integer specification upward-rounds funding and payout and produces the four documented price-scenario results. | PROVED | Deterministic scenarios and bounded fuzz properties independently prove both upward-rounded funding stages, minimal upward-rounded payout, normalization at 0/6/12/18 decimals, exact top-up/refund differences, and no overflow in the documented Phase 3B domain. |
| TECH-006 | The implemented ProofPay escrow fails closed when the XRP/USD FTSO fee is nonzero or its observation is invalid or stale. | PROVED | Oracle tests cover fee preflight ordering, `UnsupportedFtsoFee`, external-read failure, zero/future/stale/malformed values, actual feed decimals, and explicit zero-value reads. Phase 4B additionally records successful live zero-fee, fresh-price funding and release calls; failure branches remain deterministic-test evidence. |
| TECH-007 | The ProofPay escrow exposes no owner, admin, rescue, arbitrary-recipient, or unrestricted withdrawal method. | PROVED | Final production ABI and opcode inspection contains only constants/dependency getters, invoice/liability getters, quotes, and the nine specified lifecycle functions; stateful token conservation found no unaccounted recipient. |
| TECH-008 | The sampled healthy Coston2 XRP/USD feed behavior supports the planned 30-second deployment freshness setting. | PROVED | `artifacts/ftso-tolerance.json` records 24/24 successful reads over 167.118 seconds: age min/median/max `0.737/4.684/21.984s`, zero failed reads, zero nonzero fees, and 8.016 seconds of headroom. This is a bounded sample, not a future-cadence guarantee. |
| PROD-001 | ProofPay prevents an underfunded release from silently short-paying the freelancer. | PROVED | The deterministic `$0.90` case and stateful forced-underfunding action preserve `SUBMITTED` and transfer zero; bounded fuzzing proves every successful payout meets the target and one fewer atomic unit would not. |
| PROD-002 | ProofPay refunds unused FXRP protection to the client. | PROVED | Deterministic scenarios, fuzzed release flows, and stateful ghost accounting show payout plus client refund equals the prior lock and the refund is exactly `locked - payout`. |
| PROD-003 | ProofPay provides legal escrow or complete dispute resolution. | FALSE | Explicit non-goal and known limitation. |
| PROD-004 | ProofPay supports fiat or Nigerian bank settlement. | FALSE | Explicit non-goal. |
| PROD-005 | ProofPay is production-secure. | FALSE | No audit or production evidence exists. |
| PROD-006 | ProofPay automatically releases FXRP after the freelancer submits evidence. | FALSE | The named client must call release; the Phase 2 architecture honestly records that the client can refuse. |
| PROD-007 | ProofPay enforces the locked six-state invoice lifecycle and freelancer/client authorities. | PROVED | Deterministic lifecycle tests plus a 14-action stateful handler exercise allowed, repeated, invalid, terminal, and unauthorized sequences; ghost records preserve terms, evidence, and terminal states. |
| PROD-008 | Active ProofPay FXRP liabilities cannot be cross-subsidized by another invoice or a direct donation during settlement. | PROVED | Deterministic multi-invoice tests and stateful invariants keep aggregate liabilities equal to active locks, never above balance, while direct donations remain exact non-liability surplus. |
| DEPLOY-001 | A ProofPay contract is deployed on Coston2. | PROVED | `ProofPayEscrow` is confirmed at `0x53bE2D49f4bFCF2cc04A225Ccb7398Fb5E5EAA21` in successful chain-114 transaction `0xa223570423d92e6dc972452ff00da35c2d59d5c0c4c9f3a971e7cd6dabf5f93a`; receipt and explorer links are preserved in `deployment/coston2.json`. |
| DEPLOY-002 | The Coston2 deployment matches the reviewed local build and constructor configuration. | PROVED | The confirmed creation input matches the reviewed initcode hash; deployed runtime hash `0xd455d0ee1c99f901d571e25c4cf25902249097d8212d485417e7032ee3ff5338` matches the live-RPC constructor simulation; immutable getters match the officially resolved FXRP, FTSOv2, verified XRP/USD feed ID, and 30-second age limit. |
| DEPLOY-003 | The deployed escrow began with no invoice or FXRP liability. | PROVED | Post-deployment reads show `activeFxrpLiabilities == 0`, FXRP balance `0`, an empty first invoice slot, and `InvoiceNotFound(1)` from an invalid quote lookup. A `$100` live quote simulation used `eth_call` only and left those values unchanged. |
| DEPLOY-004 | The deployed Coston2 source is reproducibly verified on the explorer. | PROVED | Blockscout verification passed for Solidity `0.8.25`, optimizer `200`, IR, Cancun EVM, exact constructor arguments, and the recorded creation transaction; the full command and response are in `deployment/coston2.json`. |
| LIVE-001 | One complete ProofPay invoice settled on the deployed Coston2 contract. | PROVED | Invoice `1` moved through `CREATED`, `FUNDED`, `SUBMITTED`, and `RELEASED` in the confirmed create, funding, evidence, and release transactions recorded in `artifacts/coston2-live-invoice.json` and `docs/LIVE_RECEIPT.md`. |
| LIVE-002 | The deployed contract rejected an expired funding quote without changing live state. | PROVED | A read-only `fundInvoice` simulation decoded the exact `ExpiredQuote(1786191061, 1786191062)` data; same-block state and balances were identical, and no failing transaction was sent. |
| LIVE-003 | The live settlement conserved the invoice lock and cleared active liabilities. | PROVED | Funding locked `5.299945 FXRP`; release paid `4.818748 FXRP` and refunded `0.481197 FXRP`, whose atomic values sum exactly to the lock. Independent historical balance reads match, invoice `1` is `RELEASED`, and liabilities and contract FXRP balance are zero. |
| LIVE-004 | The live invoice binds deterministic scope and delivery evidence. | PROVED | Canonical UTF-8 manifests hash to the scope and evidence commitments stored for invoice `1`; the `EvidenceSubmitted` event emitted the source-verified Coston2 explorer page, and the independent verifier recomputed both hashes. This proves byte integrity, not delivery truth or quality. |
| UI-001 | ProofPay exposes arbitrary positive invoice IDs through current-state contract reads without scanning event history. | PROVED | `/invoice/[id]` validates canonical positive `uint256` IDs and reads the invoice, liabilities, and contract FXRP balance at one pinned block. Unit and browser checks cover invoice 1, an unknown ID, and fixture isolation; Phase 5A contains no `eth_getLogs` path. |
| UI-002 | The invoice 1 settlement receipt independently reconciles the preserved lifecycle locators against live Coston2. | PROVED | `/receipt/1` retrieves each transaction, receipt, and block; requires exactly one expected deployed-contract event per lifecycle transaction; re-reads invoice, liability, contract balance, and party balances at one pinned current block; and passed live reconciliation with payout plus refund equal to the historical lock. |
| UI-003 | The Phase 5A interface presents the verified invoice 1 settlement values without invented production facts. | PROVED | The final live browser run asserts the exact parties, deadline, commitments, manifests, prices/times, amounts, evidence URI, transaction hashes, blocks/times, network, contract, and visible pinned-read provenance. The five committed screenshots were produced only by that live run. |
| UI-004 | The Phase 5A interface is read-only and performs no wallet or chain-write action. | PROVED | The routes expose no wallet connection, signing, transaction control, form, write client, send method, indexer, or history scan. Browser tests reject transaction-action controls, and the Phase 5A call graph uses public reads plus one optional release simulation. |
| UI-005 | The primary invoice and receipt views pass automated accessibility and 390-pixel responsive checks. | PROVED | Axe passed desktop/mobile invoice and receipt views, the expanded evidence state, and the prepared repeated-top-up state; mobile checks had no horizontal overflow and keyboard focus remained visible. This is automated conformance evidence, not usability testing. |
| UI-006 | ProofPay provides a public decoded receipt for every released invoice. | PARTIAL | Invoices `1` and `2` both have verified preserved locators and fully reconciled public receipts. Arbitrary released invoices can expose current state but receive no payout/refund receipt claim without a preserved locator. |
| UI-007 | ProofPay discovers generic historical receipt transactions for arbitrary invoices. | PLANNED | Generic history discovery requires an indexing or equivalent discovery layer and remains deliberately excluded. The browser-local action journal is not a generic historical index. |
| UI-008 | ProofPay derives invoice actions from the connected role, current contract state, deadline, actual connected chain, and latest settlement quote. | PROVED | The centralized policy has deterministic unit coverage for client, freelancer, unrelated, disconnected, wrong-network, every lifecycle boundary, deadline equality, fresh-quote gating, top-up, release, and terminal states; deterministic browser flows exercise the corresponding controls. |
| UI-009 | ProofPay prepares wallet actions through an injected EVM provider on Coston2 without reading or storing private keys. | PROVED | wagmi and viem use one injected connector, require the connected account's chain ID `114`, simulate before exposing the exact signature intent, and store only public action/transaction metadata. This is code and deterministic-provider evidence, not a live wallet broadcast. |
| UI-010 | ProofPay funding uses the deployed contract's protected quote and never prepares unlimited FXRP approval. | PROVED | Unit tests independently reproduce both upward-rounded funding stages and the 10% protection, reject a contradictory quote, bound tolerance to 5%, and assert that the separate approval equals only the frozen intent maximum. Deterministic browser coverage proves one accepted maximum is reused across exact approval and funding under ordinary movement; insufficient current allowance alone drives another approval. |
| UI-011 | ProofPay evidence actions bind normalized delivery references and optional completion metadata to deterministic canonical UTF-8 JSON. | PROVED | Unit and browser tests prove URL normalization/sorting/deduplication, public-host restrictions, commit normalization, exact canonical bytes, deterministic `keccak256`, and `submitEvidence` simulation. This proves the committed bytes, not delivery truth or quality. |
| UI-012 | ProofPay prevents every duplicate or conflicting transaction across browsers and devices. | PARTIAL | The browser-local journal prevents replay of one broadcast intent, preserves wallet-open state fail-closed, reconciles submitted receipts in the same browser, and keeps one-time actions blocking. It has no server, cross-device, or indexer coordination, so the universal cross-browser/device claim is not proved. |
| UI-013 | ProofPay has completed the full wallet-originated action surface—create, exact approval, funding, evidence, top-up, release, cancellation, and refund—on live Coston2. | PARTIAL | Invoice `2` proves browser-originated create, exact approval, funding, evidence submission, and release. Live top-up, cancellation, and missed-deadline refund have not all been demonstrated; their coverage remains deterministic only. |
| LIVE-005 | A new ProofPay invoice completed create, exact approval, funding, evidence, and release through the actual browser wallet-action interface on Coston2. | PROVED | Invoice `2` has browser-originated create, four quote-driven exact approvals, funding, evidence, and release transactions in `artifacts/coston2-browser-invoice.json`; each broadcast count is one, and the completed-journal replay sent none. |
| LIVE-006 | The browser-settled invoice conserved its lock and cleared the escrow accounting. | PROVED | `artifacts/coston2-browser-settlement-receipt.json` and the public-only verifier show lock `2.126887 FXRP`, payout `1.933309`, refund `0.193578`, no top-up, state `RELEASED`, liabilities `0`, and contract balance `0`. |
| UI-014 | ProofPay exposes a verified public decoded receipt for browser-settled invoice 2. | PROVED | `/receipt/2` uses its preserved locator, verifies exact lifecycle events and current state, and passed desktop/mobile browser assertions plus independent Coston2 reconciliation. |
| UI-015 | The Phase 5B2 test signer keeps burner private keys outside the browser application. | PROVED | The test-only Playwright binding signs in the Node process; the page receives public accounts and EIP-1193 responses. Exact-key scans across source, artifacts, configuration, logs, and screenshots found no key value, and the owner-only secret-file hash was unchanged. |
| UI-016 | The Phase 5B2 live browser run proves human usability or production readiness. | FALSE | The run is automated technical evidence. It recorded repeated quote-driven approvals, a local-time deadline discrepancy, and a development hydration warning; no human usability study, audit, mainnet, or production-security evidence exists. |
| UI-017 | Invoice 2's confirmed deadline is exactly 24 hours after its creation block. | FALSE | The immutable onchain values differ by `82,853` seconds because the browser's `datetime-local` value was interpreted in local time and the deadline must be supplied before creation confirms. |
| UI-018 | New milestone deadlines are converted from an explicit IANA timezone to one reviewed Unix timestamp without implicit timezone-free parsing. | PROVED | Deadline unit tests cover Africa/Lagos, UTC, a negative offset, daylight-saving gaps and overlaps, field round-trip, exact 24-hour preset behavior, and no Lagos one-hour drift; browser coverage checks the local, UTC, and contract-second signing display. Historical invoice 2 is unchanged. |
| UI-019 | A normal funding flow reuses one accepted maximum across approval and funding instead of requoting after approval. | PROVED | The persisted funding-intent record binds invoice, account, chain, contract, preview, protected base, 2% maximum, deadline, and hash. Unit and deterministic browser tests prove one quote and one exact approval, movement within tolerance, allowance skip, reload reuse, and invalidation on expiry, amount above maximum, account, chain, or invoice change. No unlimited approval path exists. |
| UI-020 | ProofPay wallet state hydrates without a client/server markup mismatch. | PROVED | Wallet-derived state is gated behind `useSyncExternalStore`; development and production browser tests restore connected state while rejecting hydration warnings, and the first server/client state uses the same accessible loading panel. |
| UI-021 | The refined released invoice and receipt expose evidence-backed settlement hierarchy and economics for invoices 1 and 2. | PROVED | Live read-only tests assert `SETTLED`, human-first lifecycle labels, exact event/block topology, lock/payout/refund conservation, funding/release prices, percentage movement, shortened identifiers, technical `RELEASED` state, mobile summary, and seven captured desktop/mobile/action artifacts. |
| UI-022 | Phases 5C, 5D, or 6A prove ProofPay is ready for unaudited mainnet or human production use. | FALSE | The evidence is automated Coston2 testnet and deterministic-browser evidence. It adds no audit, human usability study, mainnet, legal-escrow, fiat-settlement, or production-security proof. |
| UI-023 | The browser journal permits a distinct later top-up after another deterministic price decrease while preventing the same broadcast top-up intent from being submitted twice. | PROVED | Phase 5D unit and simulated-wallet browser tests vary every identity field and cover two quote observations and shortfalls; two synchronous sign clicks produce one send, exact-hash replay is rejected, an ambiguous post-send/no-hash provider result remains unsignable across reload while explicit rejection stays retryable, unresolved submission blocks before allowance/approval, delayed receipts merge without journal loss, zero-shortfall policy, context invalidation, history retention, and one-time guards all pass. This is deterministic evidence only: invoices `1` and `2` required no top-up, and no live Coston2 repeated-top-up flow is claimed. |
| WEB-001 | ProofPay has a committed public-deployment source baseline. | PROVED | Application commit `903c36bf8d0bf172c1aaf113b46db375c4e210c7` adds the intentional root redirect, environment-aware canonical metadata, persistent Coston2 trust notice, and constrained Vercel upload inputs. This proves committed source behavior, not a reachable custom domain. |
| WEB-002 | A provider-hosted ProofPay stage serves the application and safely prepares a Coston2 create intent without broadcasting. | PROVED | Recorded Vercel Preview and corrected staged Production deployment `dpl_HYzfUxvqqiLijsY2vCaNMXP268V9` reached `READY`; bypass-backed browser checks covered application/invoice/receipt/unknown routes, current values, corrected `.xyz` canonical metadata, responsive overflow, accessibility, and browser failures. The injected provider prepared and abandoned one unsigned `createInvoice` intent with zero signing or send calls. Generated deployment protection means this is not anonymous custom-domain evidence. |
| WEB-003 | The complete role-aware wallet-action surface has been exercised against live Coston2 state from the hosted deployment. | PARTIAL | Hosted injected-wallet connection, Coston2 recognition, and safe `createInvoice` simulation are proved. The only live invoices are terminal `RELEASED`, so the hosted client/freelancer funding role, `quoteFunding`, and funding-intent screenshot cannot be exercised without broadcasting a new invoice; deterministic coverage is not substituted for public evidence. |
| WEB-004 | `proofpay.paysmat.xyz` publicly serves ProofPay over verified HTTPS. | PROVED | Phase 6A established the exact DNS-only project CNAME, authoritative/public resolution, verified conflict-free Vercel attachment, matching certificate, and HTTP-to-HTTPS redirect. Phase 6B2 changed no DNS record and maps the canonical domain to exact READY Production deployment `dpl_FAW3WmZqyeRunaxSkFqkPBu1T5Ny`; anonymous no-bypass route, responsive, sampled accessibility, wallet-simulation, read-only Coston2 reconciliation, screenshot, and bounded production-log checks passed with zero signing or broadcasts. |
| UI-024 | The Phase 6B1 Signal Ledger interface presents the locked landing, scenario, application, invoice, and receipt hierarchy without changing protected contract or wallet behavior. | PROVED | Exact scenario unit tests, one-worker deterministic wallet/browser and production-hydration suites, responsive/zoom and Axe checks, 12 baseline plus 18 visually reviewed final captures, read-only invoice-1/invoice-2 reconciliation, and protected-file checks pass. The visual manifest records zero signatures, sends, broadcasts, overflow, console errors, or page errors. This is automated implementation evidence, not human-usability or WCAG-conformance proof. |
| WEB-005 | A non-production Phase 6B1 Preview served the validated Signal Ledger interface while the Phase 6A production deployment and canonical domain remained unchanged at that checkpoint. | PROVED | Implementation commit `f43dcb886265722193f35a60e38cb7fce5ca7fe1` produced protected, unaliased Vercel Preview `dpl_zQ9sneHYfRMPimFJJSKBSs4W8sCF`, which reached `READY`/`STAGED`. `artifacts/signal-ledger/preview-proof.json` records 13 passing route checks, 13 Axe scans with zero serious/critical findings, eight responsive screenshots, exact keyboard scenario behavior, zero overflow/browser failures, and one strict unsigned wallet create simulation with zero signing, send, or broadcast calls; independent browser checks covered all routes and `320`/`1440` structural accessibility. At the Phase 6B1 checkpoint, Production deployment `dpl_HYzfUxvqqiLijsY2vCaNMXP268V9` and `https://proofpay.paysmat.xyz` remained unchanged with no promotion, DNS change, or alias. This is protected automated Preview evidence, not anonymous access, active-live-invoice, human-usability, or WCAG-conformance proof. |
| UI-025 | The Phase 6B2 Escrow Flow interface presents the causal USD agreement, FXRP lock, payout/refund, and top-up-barrier hierarchy without changing authoritative product or receipt behavior. | PROVED | Implementation commit `78cfde3f3eeb3025f8eecdc4cb2d3db69f4c3d55` preserves contract reads, wallet/action/funding/journal policy, terminal receipt locators, and fail-closed states. `artifacts/escrow-flow-final/visual-proof.json` binds 17 captures to the clean implementation head and records zero capture failure, signature request, send, actual signature, actual send, broadcast, console error, or page error. Combined deterministic gates cover exact four-scenario math, native keyboard operation, live-region updates, reduced motion, responsive widths, overflow, sampled Axe findings, and controlled failures. This is automated implementation evidence, not human-usability or WCAG-conformance proof. |
| WEB-006 | The validated Phase 6B2 Escrow Flow build is the current public deployment at `proofpay.paysmat.xyz`. | PROVED | Application commit `78cfde3f3eeb3025f8eecdc4cb2d3db69f4c3d55` produced protected READY Preview `dpl_MFtrM77aWwm3cmhaCapd8TP4qrKF` and exact READY staged/promoted Production deployment `dpl_FAW3WmZqyeRunaxSkFqkPBu1T5Ny`; anonymous canonical smoke passed after promotion with no DNS change. A bounded 1,000-record log sample starting `2026-08-12T15:30:23Z` found zero application error, fatal, or HTTP 5xx entries. Invoice 1 passed five current receipt-reconciliation checks at block `33973183`; the protected Phase 4B verifier's separate comparison of current party balances with its historical snapshot is stale after invoice 2 and is not claimed as a current pass. The isolated invoice-2 verifier passed seven checks at final block `33973412`; liabilities and contract balance remain zero, protected artifacts are unchanged, and Phase 6B2 made zero signatures, sends, or broadcasts. Phase 6A deployment `dpl_HYzfUxvqqiLijsY2vCaNMXP268V9` remains the known-good rollback. |
| PKG-001 | ProofPay has a complete evidence-first Summer Signal submission package without fictional product imagery or unsupported outward claims. | PROVED | The judge README, DoraHacks draft, judging matrix, release checklist, security/upstream records, and structured judge observation contain the requested fields and conservative boundaries. Five video documents define a 02:15 walkthrough; 13 real-product capture files and six submission-media candidates are hash-bound by JSON manifests. Every video assertion maps to an existing claims-ledger ID; generated and fictional imagery counts are zero. No final video upload or DoraHacks submission is claimed. |
| WEB-007 | A fresh unauthenticated Phase 7A production and judge-path audit completed without a wallet signature, transaction send, or serious browser failure. | PROVED | `docs/JUDGE_OBSERVATION.md` records all eight required public route states, the real invoice-2-to-receipt-to-Coston2-explorer path, return to the dominant `/app` action, `390 × 844` landing/receipt checks, five semantic clicks plus one direct navigation, zero console/page errors, and no injected wallet. This is a structured automated product audit, not human-usability or WCAG-conformance evidence. |
| TEST-001 | The frozen submission source passes the complete local web and contract validation suites and fresh read-only settlement reconciliation. | PROVED | Node `22.21.1` passed lint, typecheck, `65/65` unit tests, build, `27/27` deterministic E2E, one production-hydration E2E, `4/4` isolated live-read-only interface tests, the exact-key scan, and diff checks. Foundry `1.5.1-stable` passed format, forced build, all `69` tests, six 512-run fuzz properties, and six 128-run/depth-32 invariants. Invoice 1 passed five checks at block `33976029`; invoice 2 passed seven isolated checks at block `33976062`. The known invoice-1 legacy current-party-balance comparison remains stale after invoice 2 and is not claimed as a pass. |
| REPO-001 | The complete reviewed ProofPay history and submission package are available in a secret-clean public GitHub repository. | PROVED | `https://github.com/Samfresh-ai/proofpay` is `PUBLIC`, its `main` ref matches the reviewed `SELF` evidence commit after final push, and the annotated `summer-signal-2026-final` tag resolves to that same commit. A public recursive clone and a separate final tagged shallow clone matched the expected source/submodule pins; clean install, core README commands, link checks, and repository cleanliness passed. Full reachable-history scanning found zero high-confidence secrets or sensitive archive/credential paths. The earlier public `summer-signal-2026` tag is preserved at its earlier checklist-closing commit and was not moved. |
